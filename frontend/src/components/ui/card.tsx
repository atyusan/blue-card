// This file is no longer needed as we're using Material UI Card components
// All components should import Card, CardContent, CardHeader from '@mui/material'
// Note: CardTitle is not available in Material UI, use Typography instead

export { Card, CardContent, CardHeader } from '@mui/material';
