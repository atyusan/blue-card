// This file re-exports Material UI form components
// All components should import FormControl, FormHelperText from '@mui/material'
// Note: FormItem, FormLabel, FormMessage are not available in Material UI
// Use Box, FormLabel, and Typography instead

export { FormControl, FormHelperText } from '@mui/material';
export { FormLabel } from '@mui/material';
export { Box, Typography } from '@mui/material';
